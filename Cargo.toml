[package]
name = "beer"
version = "0.1.0"
edition = "2024"

[[bin]]
name = "beer"
path = "src/main.rs"

[dependencies]
clap = { version = "4", features = ["derive"] }
inkwell = { version = "0.9", features = ["llvm21-1"] }

[profile.release]
lto = "thin"
