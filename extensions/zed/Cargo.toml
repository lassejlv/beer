[package]
name = "beer-zed-extension"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
zed_extension_api = "0.1"

# Stand-alone — keep this crate out of the repo's outer native workspace
# (which lives in /Cargo.toml and builds `crates/*`).
[workspace]
